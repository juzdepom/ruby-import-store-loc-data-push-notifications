require 'http'

# These are the app keys for the JuliaTestApp in Bend
# Note: running ruby import.rb is not going to work because
$APP_KEY = "removed"
$APP_SECRET = "removed"
$MASTER_SECRET = "removed"

# Request authentication
# $bend = HTTP.basic_auth(:user => $APP_KEY, :pass => $MASTER_SECRET)
